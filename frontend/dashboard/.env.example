# Leave unset to use the Vite dev-server proxy in src/vite.config.js
# (recommended for local dev — avoids CORS entirely).
# Set this only when pointing the built dashboard at a deployed API.
# VITE_API_URL=http://localhost:3000
