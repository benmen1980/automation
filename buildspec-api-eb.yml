version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm ci
      - npm --prefix frontend/dashboard ci
  build:
    commands:
      - npm run test:api
      - npm --prefix frontend/dashboard run build
      - node scripts/release-artifact.js build --output dist/release-manifest.json
      - node scripts/release-artifact.js verify --manifest dist/release-manifest.json
artifacts:
  files:
    - '**/*'
  exclude-paths:
    - node_modules/**/*
    - '**/node_modules/**/*'
    - .git/**/*
  discard-paths: no
